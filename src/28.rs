fn main() {
    // Example Rust code to demonstrate basic arithmetic operations
    let sum = 5 + 3;
    println!("The sum of 5 and 3 is: {}", sum);

    // Another example with a function
    fn multiply(a: i32, b: i32) -> i32 {
        a * b
    }

    // Call the function to calculate product
    let result = multiply(4, 6);
    println!("The product of 4 and 6 is: {}", result);
}
