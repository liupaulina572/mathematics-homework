fn main() {
    // Example Rust code for mathematical operations and variables
    let x = 5;
    let y = 3.14;

    // Addition of two numbers
    let sum = x + y;
    println!("The result of adding {} and {}", y, x);
    
    // Multiplication of two numbers
    let product = x * y;
    println!("The result of multiplying {} and {}", x, y);
}
